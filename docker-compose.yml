version: '3.7'

services:
    app:
        container_name: jourlay_telegram
        build:
            context: .
        volumes:
            - .:/app
            - /app/node_modules
        command: npm run start:prod
        env_file:
            - .env
networks:
    back: